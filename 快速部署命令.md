# ⚡ 快速部署命令（复制粘贴即可）

## 第一步：检查 Git 状态

```bash
cd "/Users/kiukiu/Five Elements Test"
git status
```

## 第二步：如果是新项目，初始化 Git

```bash
# 如果显示 "not a git repository"
git init
```

## 第三步：添加所有文件

```bash
git add .
```

## 第四步：创建第一次提交

```bash
git commit -m "Initial commit: Bazi Fortune Analysis app"
```

## 第五步：连接到 GitHub（替换 YOUR_USERNAME 和 REPO_NAME）

```bash
# 先创建 GitHub 仓库，然后执行：
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 例如：
# git remote add origin https://github.com/kiukiu/bazi-fortune-analysis.git
```

## 第六步：推送到 GitHub

```bash
git branch -M main
git push -u origin main
```

**如果提示输入密码：**
- 使用 GitHub Personal Access Token（不是密码）
- 创建 Token：https://github.com/settings/tokens
- 选择 "Generate new token (classic)"
- 勾选 `repo` 权限

---

## 🎯 然后在 Render 上：

1. 访问 https://render.com
2. 点击 "New +" → "Web Service"
3. 选择你的 GitHub 仓库
4. 配置：
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app --host 0.0.0.0 --port $PORT`
   - 环境变量：`DEEPSEEK_API_KEY` = `sk-ce5f3d16c611492ca98a3fda8e77dc17`
5. 点击 "Create Web Service"
6. 等待部署完成！

---

## 📝 提示

- 所有代码都会自动从 GitHub 拉取，**不需要手动上传**
- Render 会监听 GitHub 的更改，自动重新部署
- 以后更新代码：`git add .` → `git commit -m "更新"` → `git push`

