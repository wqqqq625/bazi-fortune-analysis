# 🚀 Render 部署详细步骤（方案B）

## 📋 完整流程概览

1. ✅ 将代码推送到 GitHub
2. ✅ 在 Render 连接 GitHub 仓库
3. ✅ 配置部署设置
4. ✅ 自动部署完成

---

## 第一步：将代码推送到 GitHub

### 1.1 创建 GitHub 仓库

1. **访问 GitHub**
   - 打开 https://github.com
   - 登录你的账号（如果没有，先注册）

2. **创建新仓库**
   - 点击右上角 "+" → "New repository"
   - **Repository name**: `bazi-fortune-analysis`（或你喜欢的名字）
   - **Description**: "Bazi Fortune Analysis Web App"（可选）
   - **Visibility**: 选择 "Public" 或 "Private"
     - ⚠️ 如果选择 Private，需要在 Render 中授权访问
   - **不要**勾选 "Add a README file"（因为我们已经有了代码）
   - 点击 "Create repository"

3. **复制仓库 URL**
   - 创建后，GitHub 会显示仓库 URL
   - 类似：`https://github.com/你的用户名/bazi-fortune-analysis.git`
   - 复制这个 URL，稍后会用到

### 1.2 在本地准备 Git

打开终端（Terminal），进入你的项目目录：

```bash
cd "/Users/kiukiu/Five Elements Test"
```

### 1.3 初始化 Git（如果还没有）

检查是否已经是 Git 仓库：
```bash
git status
```

如果显示 "not a git repository"，执行：
```bash
git init
```

### 1.4 添加所有文件

```bash
# 添加所有文件到 Git
git add .

# 查看将要提交的文件
git status
```

### 1.5 创建第一次提交

```bash
git commit -m "Initial commit: Bazi Fortune Analysis app"
```

### 1.6 连接到 GitHub 仓库

```bash
# 添加远程仓库（将 YOUR_USERNAME 和 REPO_NAME 替换为你的实际值）
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 例如：
# git remote add origin https://github.com/kiukiu/bazi-fortune-analysis.git
```

### 1.7 推送到 GitHub

```bash
# 推送到 GitHub（第一次推送）
git branch -M main
git push -u origin main
```

**如果提示输入用户名和密码：**
- 用户名：你的 GitHub 用户名
- 密码：需要使用 **Personal Access Token**（不是 GitHub 密码）
  - 如何创建 Token：https://github.com/settings/tokens
  - 选择 "Generate new token (classic)"
  - 勾选 `repo` 权限
  - 复制生成的 token 作为密码

---

## 第二步：在 Render 上部署

### 2.1 访问 Render 并登录

1. 打开 https://render.com
2. 点击 "Get Started for Free"
3. 选择 "Sign up with GitHub"（推荐，这样可以直接访问你的仓库）

### 2.2 创建 Web Service

1. **点击 "New +" 按钮**（通常在右上角或左侧菜单）
2. **选择 "Web Service"**

### 2.3 连接 GitHub 仓库

1. **选择仓库**
   - Render 会显示你的 GitHub 仓库列表
   - 找到并点击 `bazi-fortune-analysis`（或你创建的名字）
   - 如果没看到，点击 "Configure account" 授权访问

2. **选择分支**
   - 通常选择 `main` 或 `master`（取决于你的默认分支）

### 2.4 配置部署设置

填写以下信息：

#### 基本信息
- **Name**: `bazi-fortune-analysis`（会自动填充，可以修改）
- **Environment**: 选择 `Python 3`
- **Region**: 选择离你最近的区域（如 `Oregon (US West)` 或 `Singapore`）

#### 构建和启动命令
- **Build Command**: 
  ```
  pip install -r requirements.txt
  ```
  
- **Start Command**: 
  ```
  gunicorn app:app --host 0.0.0.0 --port $PORT
  ```

#### 环境变量
点击 "Add Environment Variable" 添加：

1. **第一个变量**
   - **Key**: `DEEPSEEK_API_KEY`
   - **Value**: `sk-ce5f3d16c611492ca98a3fda8e77dc17`

2. **第二个变量**（可选，但推荐）
   - **Key**: `FLASK_ENV`
   - **Value**: `production`

3. **第三个变量**（可选）
   - **Key**: `PYTHON_VERSION`
   - **Value**: `3.11.0`

### 2.5 开始部署

1. 检查所有设置
2. 点击 **"Create Web Service"** 按钮
3. 等待部署（通常 2-5 分钟）

### 2.6 查看部署日志

部署过程中，你可以看到：
- 构建日志（安装依赖）
- 启动日志（运行应用）
- 如果有错误，会显示在日志中

---

## 第三步：验证部署

### 3.1 获取你的网站 URL

部署成功后，Render 会显示：
- **URL**: `https://bazi-fortune-analysis.onrender.com`（或类似）
- 这个 URL 就是你的网站地址

### 3.2 测试网站

1. 点击 URL 或复制到浏览器
2. 应该能看到你的网站首页
3. 尝试提交一个分析请求，测试功能

---

## 🔄 后续更新代码

当你修改代码后，只需要：

```bash
# 1. 添加修改的文件
git add .

# 2. 提交更改
git commit -m "描述你的更改"

# 3. 推送到 GitHub
git push

# Render 会自动检测到更改并重新部署！
```

---

## ⚠️ 常见问题

### Q1: 部署失败怎么办？

**检查：**
1. 查看 Render 的构建日志，找到错误信息
2. 常见错误：
   - `ModuleNotFoundError`: 检查 `requirements.txt` 是否包含所有依赖
   - `Port already in use`: 确保使用 `$PORT` 环境变量
   - `Import error`: 检查文件路径是否正确

### Q2: 如何查看日志？

在 Render 的 Web Service 页面：
- 点击 "Logs" 标签
- 可以看到实时日志和错误信息

### Q3: 网站访问很慢？

Render 免费版：
- 15 分钟无活动后会休眠
- 首次访问需要几秒唤醒
- 这是正常的，升级到付费版可以保持常驻

### Q4: 如何绑定自定义域名？

1. 在 Render 的 Web Service 设置中
2. 点击 "Custom Domains"
3. 添加你的域名
4. 按照提示配置 DNS 记录

---

## ✅ 检查清单

部署前确认：
- [ ] 代码已推送到 GitHub
- [ ] GitHub 仓库是 Public 或已授权 Render 访问
- [ ] `requirements.txt` 包含所有依赖
- [ ] `Procfile` 存在且正确
- [ ] `runtime.txt` 指定了 Python 版本
- [ ] 环境变量已设置（DEEPSEEK_API_KEY）
- [ ] Build Command 正确
- [ ] Start Command 正确
- [ ] 部署成功
- [ ] 网站可以访问
- [ ] 功能测试通过

---

## 🎉 完成！

部署完成后，你的网站就可以通过 Render 提供的 URL 访问了！

**下一步：在 Wix 中嵌入或链接这个 URL**

